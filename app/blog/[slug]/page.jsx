'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { useParams } from 'next/navigation';
import { getAllBlogPosts } from '../blogData';
export default function BlogDetailsPage(){ const params=useParams(); const [post,setPost]=useState(null); useEffect(()=>{setPost(getAllBlogPosts().find(item=>item.slug===params.slug)||null);},[params.slug]); if(!post){return <main><div className="site-bg"/><section className="section-shell blog-hero reveal-item is-visible"><p className="section-kicker">Blog</p><h1>Article not found.</h1><Link className="btn primary" href="/blog">Back to Blog</Link></section></main>;} return <main><div className="site-bg"/><div className="cursor-glow"/><header className="navbar"><Link href="/" className="brand"><span className="brand-mark">RA</span><span>Ravindu</span></Link><nav><Link href="/">Portfolio</Link><Link href="/blog">Blog</Link></nav></header><article className="section-shell blog-detail reveal-item is-visible"><Link href="/blog" className="back-link">← Back to Blog</Link><div className="blog-detail-cover" style={{backgroundImage:`url(${post.image})`}}/><div className="blog-detail-content"><div className="blog-meta"><span>{post.category}</span><span>{post.date}</span><span>{post.readTime}</span></div><h1>{post.title}</h1><p className="blog-detail-excerpt">{post.excerpt}</p><div className="article-body">{post.content.split('\n').filter(Boolean).map((paragraph,index)=><p key={index}>{paragraph}</p>)}</div><div className="author-box"><strong>K.G. Ravindu Ashan Dhananjaya</strong><span>Associate Software Engineer | Full-Stack Developer</span></div></div></article></main>; }
